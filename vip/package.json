{
  "name": "vip",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "predev": "npm --prefix .. run config:generate && npm --prefix .. run projectiles:generate",
    "dev": "vite",
    "prebuild": "npm --prefix .. run config:generate && npm --prefix .. run projectiles:generate",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pretest:headless": "npm --prefix .. run config:generate && npm --prefix .. run projectiles:generate",
    "test:headless": "tsc -p tsconfig.headless.json && node .headless-dist/vip/scripts/headless-default-template-smoke.js && npm --prefix ../arena run verify:ai-rules"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}
